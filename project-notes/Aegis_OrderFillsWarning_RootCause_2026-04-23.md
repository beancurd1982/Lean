# Aegis OrderFills Warning Root Cause Investigation - 2026-04-23

## 2026-04-23 - Step 1: Start Investigation

Summary:
- Started investigation of persistent `OrderFillsDuringExtendedMarketHoursAnalysis` warning after V7 and V8 both showed the warning count equals exactly `2 x Total Orders`.
- Scope is investigation only. No algorithm execution or strategy logic changes are planned until root cause is identified.

Files touched:
- `project-notes/Aegis_OrderFillsWarning_RootCause_2026-04-23.md`

Open questions:
- Is the warning identifying actual extended-hours fills, or is the report analyzer counting all submitted/filled order events because of metadata/time-zone interpretation?
- Where is `OrderFillsDuringExtendedMarketHoursAnalysis` implemented or generated?

## 2026-04-23 - Step 2: Broad Search Result

Summary:
- Ran a broad repository search for `OrderFillsDuringExtendedMarketHoursAnalysis`, `ExtendedMarketHours`, and market-hours warning text.
- The search was too broad and matched large backtest JSON content, so the result was noisy and not useful as-is.
- A direct local implementation name match was not visible in the useful part of the output.

Files touched:
- `project-notes/Aegis_OrderFillsWarning_RootCause_2026-04-23.md`

Next step:
- Narrow the search to source files and report/statistics/analyzer-related code only.

## 2026-04-23 - Step 3: Narrow Source Search Result

Summary:
- Narrowed the search to source and config files while excluding Aegis backtest artifacts.
- No local source implementation of `OrderFillsDuringExtendedMarketHoursAnalysis` was found.
- Report/statistics-related local code exists under `Report/` and `Common/Statistics/`, but the named warning appears to be generated by QuantConnect Cloud analysis rather than this local LEAN checkout.
- Current working tree remains uncommitted; no source changes were made during this investigation step.

Files touched:
- `project-notes/Aegis_OrderFillsWarning_RootCause_2026-04-23.md`

Next step:
- Extract the exact warning payload and order-event samples from V7/V8 JSON to verify whether the warning count tracks order events rather than true extended-hours fills.

## 2026-04-23 - Step 4: JSON Structure Check

Summary:
- Parsed `Logs_V7.json` and `Logs_V8.json`.
- Both files have top-level `analysis` lists and top-level `orders` dictionaries.
- `Logs_V7.json` has `1349` orders.
- `Logs_V8.json` has `1168` orders.
- This matches the earlier observed pattern where the warning count was exactly `2 x Total Orders`.

Files touched:
- `project-notes/Aegis_OrderFillsWarning_RootCause_2026-04-23.md`

Next step:
- Inspect `analysis` records and representative order records to determine which event statuses and timestamps are being counted.

## 2026-04-23 - Step 5: Analysis Payload Finding

Summary:
- `Logs_V7.json` warning sample:
  - `name=OrderFillsDuringExtendedMarketHoursAnalysis`
  - `count=2698`
  - sample `status=submitted`
  - sample `fillPrice=0.0`
  - sample `fillQuantity=0.0`
  - sample `time=1514905200.0`, which is `2018-01-02T15:00:00Z`
- `Logs_V8.json` warning sample has the same structure:
  - `count=2336`
  - sample `status=submitted`
  - sample `fillPrice=0.0`
  - sample `fillQuantity=0.0`
  - sample `time=1514905200.0`
- The top-level `orders` dictionary does not include embedded order events; it only includes final order records.
- Since the sample event is `submitted`, the analyzer is not strictly sampling actual filled events despite the issue text saying "filled orders".

Files touched:
- `project-notes/Aegis_OrderFillsWarning_RootCause_2026-04-23.md`

Current hypothesis:
- The QuantConnect Cloud analyzer is counting both submitted and filled order events, or otherwise using event rows where `status=submitted`, and is labeling them as extended-hours fills.
- Because `count` equals exactly `2 x orders` in V7 and V8, each order likely contributes two analyzer rows.

Next step:
- Inspect raw log order-event lines to confirm submitted/filled timestamps are regular session times.

## 2026-04-23 - Step 6: Raw Log Order-Event Check

Summary:
- Searched V7 and V8 raw `.txt` logs for order-event/status keywords.
- The downloaded raw logs do not contain individual submitted/filled order-event lines.
- The current algorithm contains an `OnOrderEvent` handler, but it does not emit debug output for every order event.

Files touched:
- `project-notes/Aegis_OrderFillsWarning_RootCause_2026-04-23.md`

Next step:
- Read `OnOrderEvent` and `ExecutePlan` to verify what guards are active and whether adding temporary instrumentation would be useful for the next cloud run.

## 2026-04-23 - Step 7: Algorithm Guard Review

Summary:
- `WeeklyReview()` still checks `IsMarketOpen(_marketSymbol)` before planning orders.
- `ExecutePlan()` checks `IsMarketOpen(target.Key)` before each `SetHoldings()` call.
- `SetHoldings()` remains the only order-submission path in the reviewed execution path.
- `OnOrderEvent()` only persists live-trading state and does not emit per-event debug lines in backtests.

Files touched:
- `project-notes/Aegis_OrderFillsWarning_RootCause_2026-04-23.md`

Next step:
- Classify all V7/V8 final order timestamps in New York time to verify whether any final order records are outside regular market hours.

## 2026-04-23 - Step 8: Order Timestamp Classification

Summary:
- Classified every final order timestamp in `Logs_V7.json` and `Logs_V8.json` into America/New_York time.
- `Logs_V7.json`:
  - `1349` final orders.
  - `869` at `10:00 EDT`.
  - `480` at `10:00 EST`.
  - `0` orders outside a simple weekday `09:30-16:00` regular-hours check.
- `Logs_V8.json`:
  - `1168` final orders.
  - `712` at `10:00 EDT`.
  - `456` at `10:00 EST`.
  - `0` orders outside a simple weekday `09:30-16:00` regular-hours check.
- Tuesday orders are expected because exchange-aware `WeekStart` rolls holiday Mondays to the next trading day.

Files touched:
- `project-notes/Aegis_OrderFillsWarning_RootCause_2026-04-23.md`

Current conclusion:
- The warning is very likely not caused by Aegis placing final orders outside regular US equity market hours.
- The strongest evidence is:
  - The cloud warning sample is a `submitted` event with `fillQuantity=0.0`, not a fill.
  - Warning count equals exactly `2 x Total Orders`.
  - Final order timestamps are all `10:00` New York time.
  - US equity regular trading hours are `09:30-16:00` New York time.

## 2026-04-23 - Step 9: Official Documentation Cross-Check

Summary:
- Checked current QuantConnect documentation references relevant to the evidence:
  - Market Orders: `https://www.quantconnect.com/docs/v2/writing-algorithms/trading-and-orders/order-types/market-orders`
  - Market Hours: `https://www.quantconnect.com/docs/v2/writing-algorithms/securities/market-hours`
  - US Equity Market Hours: `https://www.quantconnect.com/docs/v2/writing-algorithms/securities/asset-classes/us-equity/market-hours`
- The docs state US equity regular trading hours are `09:30:00` to `16:00:00` America/New_York.
- The docs state market orders are the normal order type for immediate fills and that outside-hours market orders can be converted to market-on-open orders.
- Aegis V7/V8 final order records are at `10:00` America/New_York, which is inside the documented regular session.

Files touched:
- `project-notes/Aegis_OrderFillsWarning_RootCause_2026-04-23.md`
- `project-notes/Aegis_Warning_Fix_Plan_2026-04-21.md`

Recommendation:
- Do not make another execution-path change to chase this warning.
- Keep V7 schedule-only.
- If more evidence is required, use a temporary diagnostic cloud run that logs order event status, fill quantity, fill price, algorithm time, UTC time, and each symbol's regular-hours open status.
- Otherwise, ask QuantConnect support/forum whether `OrderFillsDuringExtendedMarketHoursAnalysis` is expected to flag `status=submitted`, `fillQuantity=0.0` order events at `10:00 AM` New York time.

## 2026-04-23 - Review And Verification

Strict review:
- Reviewed the source diff for `AegisGrowthAllocation.cs`; no new algorithm code changes were made during this root-cause investigation.
- The remaining algorithm source diff is still the V7 schedule-only change: exchange-aware `WeekStart` plus `AfterMarketOpen`.
- Reviewed the warning-fix plan update; it now reflects the evidence that V8 did not fix the warning and that the cloud analyzer sample is a submitted non-fill event.
- No safety/correctness issue found in the documentation updates.

Verification:
- Verified `Logs_V7.json` order count is `1349`; warning count is `2698`.
- Verified `Logs_V8.json` order count is `1168`; warning count is `2336`.
- Verified all V7/V8 final order timestamps are `10:00` America/New_York and none are outside a simple weekday `09:30-16:00` regular-hours check.
- Verified local source search did not find an implementation of `OrderFillsDuringExtendedMarketHoursAnalysis` in this checkout.

Residual risks:
- QuantConnect Cloud may apply internal analyzer rules that are not publicly documented or present in this repository.
- The downloaded JSON does not include the full analyzer event list, only a sample and count.
- A diagnostic cloud run with temporary order-event logging could provide stronger evidence if QuantConnect support asks for it.

## 2026-04-23 - Step 10: Publish And Cleanup

Summary:
- Preparing to commit and push the V7 schedule-only baseline, V7/V8 backtest evidence, warning root-cause notes, and revised warning-fix plan.
- Scope excludes any new execution-path change beyond the already accepted V7 exchange-aware schedule.

Files expected in commit:
- `Algorithm.CSharp/MyAlgorithms/AegisGrowthAllocation/AegisGrowthAllocation.cs`
- `Tests/Algorithm/AegisGrowthAllocationTests.cs`
- `Algorithm.CSharp/MyAlgorithms/AegisGrowthAllocation/BackTestLogs/log-index.csv`
- `Algorithm.CSharp/MyAlgorithms/AegisGrowthAllocation/BackTestLogs/Logs_V7.json`
- `Algorithm.CSharp/MyAlgorithms/AegisGrowthAllocation/BackTestLogs/Logs_V8.json`
- `project-notes/Aegis_Execution_Realism_Fix_2026-04-21.md`
- `project-notes/Aegis_Warning_Fix_Plan_2026-04-21.md`
- `project-notes/Aegis_Backtest_Log_Analysis_2026-04-22.md`
- `project-notes/Aegis_Backtest_Log_Analysis_2026-04-23.md`
- `project-notes/Aegis_OrderFillsWarning_RootCause_2026-04-23.md`
- `project-notes/Aegis_QC_Docs_Reassessment_2026-04-22.md`

Review requirement:
- Re-check the staged diff before committing.
- Confirm the working tree is clean after push.

## 2026-04-23 - Step 11: Pre-Commit Review And Verification

Review:
- Reviewed the intended source diff. The only algorithm behavior change is the V7 exchange-aware weekly schedule.
- Reviewed `Tests/Algorithm/AegisGrowthAllocationTests.cs`. It only asserts that the weekly review schedule uses exchange-aware `WeekStart` / `MarketOpen` naming instead of generic `Every` / `10:00`.
- Reviewed `BackTestLogs/log-index.csv`; the V7 and V8 normalized logs are marked `reviewed` and point to their project-note analysis files.

Verification:
- Searched `AegisGrowthAllocation.cs` and `AegisGrowthAllocationTests.cs` for V8-only markers: `_pendingPlan`, `TryExecutePendingPlan`, `Weekly review queued`, `Weekly rebalance executed`, `Resolution.Minute`, and `Consolidate(...)`.
- Result: no matches, confirming the V8 execution-path code is not present.
- Parsed `Logs_V7.json` and `Logs_V8.json`:
  - V7: `orders=1349`, `warning_count=2698`, sample `status=submitted`, sample `fill_qty=0.0`.
  - V8: `orders=1168`, `warning_count=2336`, sample `status=submitted`, sample `fill_qty=0.0`.
- Attempted focused local .NET test:
  - `dotnet test Tests/QuantConnect.Tests.csproj --no-restore --filter "AegisGrowthAllocationTests.RegistersExchangeAwareWeeklyReviewSchedule" -p:NuGetAudit=false -p:TreatWarningsAsErrors=false -p:WarningsAsErrors=''`
  - First run failed because the sandboxed default .NET home could not write to `C:\Users\CodexSandboxOffline\.dotnet`.
  - Retried with workspace-local `DOTNET_CLI_HOME`.
  - Retry still exited non-zero before a usable test result, showing existing NuGet audit warnings and first-run tooling output.

Residual verification gap:
- Local test execution remains blocked by this repo/tooling state.
- QuantConnect Cloud remains the authoritative compile/backtest verification target for the algorithm source.

## 2026-04-23 - Step 12: Staged Diff Review

Summary:
- Staged 11 files for commit.
- Staged scope includes only Aegis source, Aegis schedule regression test, V7/V8 JSON reports, backtest log index, and project notes.
- The ignored normalized `.txt` backtest logs remain local-only as intended by `BackTestLogs/.gitignore`.

Staged diff summary:
- `AegisGrowthAllocation.cs`: V7 exchange-aware schedule only.
- `AegisGrowthAllocationTests.cs`: schedule registration regression test.
- `Logs_V7.json` / `Logs_V8.json`: cloud report JSON evidence.
- `log-index.csv`: V7/V8 reviewed entries.
- `project-notes/`: V7/V8 analysis, docs reassessment, root-cause investigation, and revised warning plan.

Review result:
- No unrelated staged files found.
- Proceeding to commit and push.
